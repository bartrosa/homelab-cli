// Package podman installs, configures, updates and removes the Podman container
// runtime. Installation delegates to OS package managers; configuration handles
// rootless prerequisites, Quadlet directories, registries and systemd lingering.
package podman

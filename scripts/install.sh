#!/usr/bin/env bash
set -euo pipefail

# TODO: implement after the first GitHub Release exists.
# This script should download the `lab` binary for the current OS/arch,
# verify checksums against the published SHA256 file, and install into /usr/local/bin (or $HOME/.local/bin).

echo "install.sh is not implemented yet (waiting for first release artifacts)." >&2
exit 1
